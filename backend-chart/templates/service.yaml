apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: helm-project

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}